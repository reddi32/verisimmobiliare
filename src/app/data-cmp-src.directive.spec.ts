import { DataCmpSrcDirective } from './data-cmp-src.directive';

describe('DataCmpSrcDirective', () => {
  it('should create an instance', () => {
    const directive = new DataCmpSrcDirective();
    expect(directive).toBeTruthy();
  });
});
